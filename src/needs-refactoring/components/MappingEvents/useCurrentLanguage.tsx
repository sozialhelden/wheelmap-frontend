import * as React from "react";

export function useCurrentLanguage(): { preferredLanguage: string } {
  // TODO: Mock implementation. Use React.useContext() here.
  const preferredLanguage = "en";

  return { preferredLanguage };
}
